# Changelog

## Unreleased

Changes targeting the next release only. Release-specific changes move here automatically when the version is cut.

## v0.14.0

- feat(visual-v2): merge compatibility foundation

## v0.13.0

- fix(ci): repair promotion and automatic release
- chore: promote dev to main
- Merge Phase 1 Quanta foundation guardrails into dev
- chore(dist): remove stale playground delivery output
- chore(dist): remove stale playground delivery output
- chore(dist): remove stale playground delivery output
- ci: remove temporary runtime asset synchronizer
- assets(ui): sync accepted runtime assets v1.7.0
- fix(types): bound generated asset registry declaration
- ci: perform explicit runtime asset sync
- ci: remove temporary visual baseline refresher
- test(visual): refresh intentional Quanta baselines
- ci: refresh intentional visual baselines
- ci: restore generated dist sync target
- chore(dist): synchronize generated delivery artifacts
- ci: sync accepted asset head delivery artifacts
- chore(assets): acknowledge reviewed Visual System 2.0 upstream head
- chore(dist): converge generated CSS fixed point
- ci: restore generated dist sync target
- chore(dist): synchronize generated delivery artifacts
- refactor(ui): clear application screen design-value debt
- ci: temporarily sync Quanta generated dist
- chore(dist): sync navigation variant bundle
- fix(a11y): use neutral navigation button variants
- chore(todo): reconcile verified repository hardening tasks
- chore(dist): match deterministic CSS output
- ci: restore generated dist sync target
- chore(dist): synchronize generated delivery artifacts
- ci: temporarily sync feature dist
- refactor(ui): clear touched button token debt

## v0.12.5

- Hardened Git and HTTP freshness operations with bounded fail-closed provenance checks.
- Added strict UI contract audits for tokens, architecture, accessibility, assets, package artifacts, consumers, and release closure.
- Added canonical semantic asset registry and production screen coverage contracts.
- Removed historical compatibility primitive exports in favor of canonical primitives.

## v0.12.4

- Added cinematic hero local fallback and deterministic single-worker browser verification.

## v0.12.3

- Added resilient runtime-motion fallback and remote-to-local asset fallback regression coverage.
