# Changelog

## Unreleased

- Removed the historical `components/compat` primitive adapters and migrated package screens, stories, and tests to canonical primitives.
- Added `IconButton`, `VisuallyHidden`, `RegistryAsset`, and the pinned 44-pack asset registry contract.
- Added token, architecture, accessibility, consumer, and adapter audits for the canonical surface.
- Added DataTable extensions for filtering, pagination, bulk actions, visibility, density, expansion, and resilient states.
