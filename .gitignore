node_modules
# The Git dependency is a supported consumer distribution. Its export map
# points at these release artifacts, so they must travel with an immutable
# commit rather than being rebuilt in every consumer installation.
!dist/
!dist/**
storybook-static
test-results
artifacts/audit/
.DS_Store
*.log
.env
.env.*
!.env.example
